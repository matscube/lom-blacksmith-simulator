import { describe, it, expect } from 'vitest';

describe(`属性抵抗値`, () => {
  it('example', () => {
    expect(1).toEqual(1);
  });
  // TODO: add tests
  // 魔女、魔法使いよる属性抵抗値の変化
  // 魔女、魔法使いよる属性抵抗値の変化のタイミング
  // 光/闇との併用
});
